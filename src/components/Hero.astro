---
const stats = [
  { value: '35', suffix: 'M+', label: 'Monthly Reach' },
  { value: '1', suffix: 'M+', label: 'AI Community' },
  { value: '5', suffix: '', label: 'Live Platforms' },
  { value: '3', suffix: '', label: 'Graduate Degrees' },
];
---

<section class="hero" id="hero">
  <div class="hero__inner container">
    <div class="hero__text hero-enter">
      <span class="eyebrow">AI Educator &middot; Founder &middot; Development Professional</span>
      <h1 class="hero__heading">
        The next billion<br />
        <em>deserve AI too.</em>
      </h1>
      <p class="hero__lead">
        Building the platforms, communities, and infrastructure that make artificial intelligence accessible to people the tech world consistently forgets to include.
      </p>
      <div class="hero__stats">
        {stats.map((stat) => (
          <div class="hero__stat">
            <span
              class="hero__stat-value"
              data-count={stat.value}
              data-suffix={stat.suffix}
            >
              0{stat.suffix}
            </span>
            <span class="hero__stat-label">{stat.label}</span>
          </div>
        ))}
      </div>
      <div class="hero__cta">
        <a href="#partner" class="btn btn-primary btn-arrow">
          Partner With Me
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#story" class="btn btn-outline btn-arrow">
          Read the Story
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M8 3v10M4 9l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
      </div>
    </div>

    <div class="hero__visual hero-enter" style="animation-delay: 0.3s;">
      <div class="hero__photo-wrapper">
        <picture>
          <source srcset="/hero.webp" type="image/webp" />
          <img
            class="hero__photo"
            src="/hero.jpg"
            alt="Qaisar Roonjha standing in green fields, wearing an Urdu AI polo shirt"
            width="2344"
            height="1320"
            loading="eager"
            fetchpriority="high"
          />
        </picture>
        <div class="hero__overlay"></div>
        <blockquote class="hero__quote">
          <p>&ldquo;The most important work in AI isn&rsquo;t building the next model &mdash; it&rsquo;s ensuring the next billion people know how to use it.&rdquo;</p>
          <cite>&mdash; Qaisar Roonjha</cite>
        </blockquote>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 5rem;
    padding-bottom: 3rem;
    overflow: hidden;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 55% 45%;
    gap: 3rem;
    align-items: center;
  }

  .hero__text {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }

  .hero__heading {
    line-height: 1.08;
  }

  .hero__heading em {
    font-style: italic;
    font-weight: 300;
    color: var(--rust);
  }

  .hero__lead {
    font-size: 1.0625rem;
    line-height: 1.85;
    color: var(--muted);
    max-width: 50ch;
  }

  .hero__stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    border: 1px solid rgba(181, 169, 154, 0.3);
    border-radius: var(--radius);
    overflow: hidden;
    max-width: 540px;
  }

  .hero__stat {
    padding: 1rem 0.75rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .hero__stat:not(:last-child) {
    border-right: 1px solid rgba(181, 169, 154, 0.3);
  }

  .hero__stat-value {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--ink);
    line-height: 1.2;
  }

  .hero__stat-label {
    font-size: 0.6875rem;
    color: var(--faint);
    letter-spacing: 0.02em;
    line-height: 1.3;
  }

  .hero__cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Visual / Photo column */
  .hero__photo-wrapper {
    position: relative;
    border-radius: 8px;
    overflow: hidden;
  }

  .hero__photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 20%;
    display: block;
    filter: contrast(1.04) saturate(0.9);
    min-height: 500px;
    transition: transform 6s ease;
  }

  .hero__photo-wrapper:hover .hero__photo {
    transform: scale(1.02);
  }

  .hero__overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(to top, rgba(13, 13, 13, 0.85) 0%, transparent 100%);
    pointer-events: none;
  }

  .hero__quote {
    position: absolute;
    bottom: 1.5rem;
    left: 1.5rem;
    right: 1.5rem;
    background: rgba(13, 13, 13, 0.7);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-left: 3px solid var(--gold);
    padding: 1.25rem 1.5rem;
    border-radius: 4px;
  }

  .hero__quote p {
    font-family: var(--font-display);
    font-size: 0.95rem;
    font-style: italic;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
    max-width: none;
  }

  .hero__quote cite {
    display: block;
    margin-top: 0.5rem;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-style: normal;
    color: var(--gold);
    letter-spacing: 0.05em;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero__inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .hero__text {
      order: 1;
    }

    .hero__visual {
      order: 2;
      max-width: 480px;
    }

    .hero__stats {
      max-width: 480px;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 5.5rem;
      min-height: auto;
    }

    .hero__visual {
      display: none;
    }

    .hero__stats {
      grid-template-columns: repeat(2, 1fr);
      max-width: 320px;
    }

    .hero__stat:nth-child(1),
    .hero__stat:nth-child(2) {
      border-bottom: 1px solid rgba(181, 169, 154, 0.3);
    }

    .hero__stat:nth-child(2) {
      border-right: none;
    }

    .hero__stat:nth-child(4) {
      border-right: none;
    }
  }
</style>
